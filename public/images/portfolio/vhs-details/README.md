# VHS detail imagery

One dedicated image per portfolio project. Reference assets from project data with:

`/images/portfolio/vhs-details/<project-slug>.<extension>`

Prefer SVG, WebP, or AVIF. Keep the important subject inside the left two-thirds because the detail overlay applies a procedural diagonal blend into the project accent color.
