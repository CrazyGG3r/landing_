    // animationFrames.js
    // ─────────────────────────────────────────────────────────────────────────────
    // Each frame is an array of strings, rows top → bottom.
    // Strings use the  "'  content  '"  format — FaultyTerminal strips the outer
    // single-quotes automatically, so you can paste directly from your source data.
    // Every frame is 100 chars wide × 39 rows tall (plus one empty sentinel "''").
    // ─────────────────────────────────────────────────────────────────────────────

export const ANIMATION_FRAMES =[];

export const box = [];

export const third = [];